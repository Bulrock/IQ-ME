# Story bridge-7-8-5-post-epic-app-modules: Post-epic app-modules-bytes trim pass + re-establish principled ceiling

Status: backlog

## Story

app-modules-bytes grew 30720 -> 49152 (~60%) across five consecutive per-story raises in epic-6 (6.2, 6.4, 6.5, 6.7), each individually justified but never audited as a whole; the budget has decayed toward a rubber stamp (6-7 round-1). This is the exact "refactor-or-revise" trigger the existing epic-3 byte-budget SLI lesson prescribes as a Bridge-Plan item. Scope: audit src/assessment/** for trim/shared-util opportunities (e.g. duplicated escape / DOM helpers), recover headroom, and re-pin a principled ceiling BEFORE Epic-7 adds i18n locale-loading weight on top. Lands before epic-7; app-side bridge (not TDS-module).

## Sources (deferred from)

- `6-7-opt-in-localstorage-save-retest-effect-copy-on-result-page` (kind=auditor-finding, round=1, finding_index=1)

## Acceptance Criteria

1. `src/assessment/**` is audited for trim / shared-util opportunities (e.g. duplicated escape / DOM helpers across `save-result.js`, `theme.js`, `result.js`, tail-scenes); genuine duplications are consolidated into shared util(s) where it reduces total bytes **without behavior change**.
2. The measured `app-modules-bytes` size is **reduced** from the epic-6-close value (49152), recovering documented headroom — a principled reduction, not a token trim.
3. The `app-modules-bytes` ceiling in `tests/scaffold/cognitive-load-budget.test.mjs` (currently `limit: 49152`, line ≈23) is **re-pinned to a principled value** reflecting the post-trim size + a justified margin, with a rationale comment that supersedes the five ad-hoc per-story raises (6.2/6.4/6.5/6.7); the new ceiling is set **before** Epic-7 i18n locale-loading weight lands.
4. All unit tests remain green; no behavioral regression in `src/assessment/**`; **class-A integrity re-recorded** for the budget scaffold test after the limit change.

## Tasks / Subtasks

- [ ] Task 1 — Audit `src/assessment/**` (`save-result.js`, `theme.js`, `result.js`, tail-scenes, …); inventory duplicated escape / DOM helpers and measure current `app-modules-bytes` (AC 1, 2).
- [ ] Task 2 — Open an unfreeze window for `tests/scaffold/cognitive-load-budget.test.mjs` (class-A) and set the re-pinned principled ceiling reflecting the target post-trim size — the budget test now fails against the un-trimmed code (AC 3).
- [ ] Task 3 — Implement trim / shared-util consolidation in `src/assessment/**`; make the budget test pass at the lower ceiling with no behavior change (AC 1, 2).
- [ ] Task 4 — Run the full unit suite; confirm green + no regression; re-record class-A integrity for the budget scaffold test; document the principled-ceiling rationale (AC 3, 4).

## Dev Agent Record

### Completion Notes List

_(populated during implementation)_

### File List

_(populated during implementation)_

---
Generated by `tds epic create-bridge-from-retros` for bridge `bridge-7-8` (blocks `epic-8`).
