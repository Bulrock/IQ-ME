---
id: 8-5-full-playwright-trust-verification-suite-consolidation
title: "Story 8.5: Full Playwright trust-verification suite consolidation"
status: ready-for-dev
---

# Story 8.5: Full Playwright trust-verification suite consolidation

## Story

As a **skeptic running the full verification pass before recommending the project**,
I want **the complete Playwright + CSP + viewport-overflow + Lighthouse + axe-core/pa11y suite running on every PR and on the live deployment (FR41 full DevTools-verifiable surface)**,
so that **every PR mechanically asserts the project remains *as-described*: zero third-party, strict CSP, no horizontal scroll at any viewport, performance budgets met, WCAG 2.2 AA contrast**.

> **Epic-8 dev-phase decision — STRUCTURAL contract, not a live 90s CI run.** The structural deliverable is real and dev-testable: the consolidated `tests/playwright/trust-verification.spec.mjs` + `tests/perf/lighthouserc.json` + `tests/a11y/*.spec.mjs` authored, and `pr-checks.yml` jobs wired/activated. The HEAVY live runs (the full <90s Playwright pass, `lhci autorun` against a chromium-emulated mid-tier Android Slow-4G profile, axe against every methodology + SPA surface) execute **in CI** — in dev they are too slow / chromium-throttling-dependent to gate on. **The dev-verifiable frozen-test contract is therefore STRUCTURAL** (asserted by `tests/scaffold/ci-matrix.test.mjs` graduation + a NEW `tests/scaffold/trust-verification-suite.test.mjs`): the spec/config files exist, are well-formed, and assert the right things (network-trace + CSP-violation + viewport-width iteration; the four Lighthouse budgets; a11y specs wired); and `pr-checks.yml` carries the activated jobs (present, no `if: false`, per-spec wiring). A Playwright spec MAY be smoke-run locally if chromium is available, but **passing a live 90s CI run is NOT a dev gate** — the frozen tests assert structure, never a green live run. Same infra-now posture as Story 8.1 (release.yml) / Story 7.8 (branch-protection doc).
>
> **pr-checks.yml job-status reconciliation (verify before editing — this diverges from the epic's loose phrasing).** The epic AC calls `trust-verification-full` "the slot from Epic 1" and implies `lighthouse` + `cropping-fuzzer` are deferred. The actual repo state (read at authoring time) is different and is authoritative:
> | Job | epic implies | ACTUAL state in pr-checks.yml | this story does |
> | --- | --- | --- | --- |
> | `trust-verification-full` | "slot from Epic 1" | **ABSENT** — no such job exists in pr-checks.yml and it is **NOT** in `ci-matrix.test.mjs` `ALL_JOBS`. There is no Epic-1 slot. | **ADD** as a NEW active job + add to `ALL_JOBS` + `EPIC_1_ACTIVE` (class-A graduation). |
> | `lighthouse` | activate | **ALREADY ACTIVE** (Story 6.4) — runs `lighthouse@12 --only-categories=accessibility` ≥0.90 on light+dark. It does NOT enforce FCP/LCP/TTI/CLS perf budgets. | **REWIRE / extend** — add the `lhci autorun tests/perf/lighthouserc.json` perf-budget run; do NOT flip an `if:` (there is none). |
> | `cropping-fuzzer` | "gate on bankFrozen" | **ALREADY ACTIVE** (Story 6.6); self-gates on `corpus/manifest.json` `bankFrozen` which is **already `true`**. | Confirm it runs forward + is referenced by the consolidated suite. No activation flip needed. |
> | `csp-violation-count` | activate | **DEFERRED** (`if: false`, "Activates in Epic 3" comment, never activated). | **ACTIVATE** (remove `if: false`, wire real run) + move to `EPIC_1_ACTIVE`. |
> | `viewport-overflow` | activate | **DEFERRED** (`if: false`, "Activates in Epic 6", never activated). | **ACTIVATE** + move to `EPIC_1_ACTIVE`. |
> | `axe-core-pa11y` | activate | **DEFERRED** (`if: false`, "Activates in Epic 6", never activated). | **ACTIVATE** + move to `EPIC_1_ACTIVE`. |
>
> **"No new jobs after Epic 1" tension — resolved.** pr-checks.yml's header asserts subsequent epics activate stubs, never add jobs. `trust-verification-full` has no stub to activate, yet the AC names it. Resolution (engineer to honor, escalate if disputed): treat `trust-verification-full` as a net-new consolidated job this story OWNS adding, and graduate the frozen `ci-matrix.test.mjs` `ALL_JOBS`/`EPIC_1_ACTIVE` accordingly. This is the exact pattern lesson-2026-06-03-002 warns about (activating/adding CI jobs without updating the frozen job-set → self-inflicted red), so the ci-matrix graduation is mandatory and integrity-re-registered.

## Acceptance Criteria

**AC-1 (consolidated trust-verification.spec.mjs — network-trace + CSP-violation + viewport-overflow):** `tests/playwright/trust-verification.spec.mjs` exists and consolidates three assertions driven over the full SPA happy-path (landing → consent → 16-item seeded session → reveal → result; same `window.__IQME_TEST__` `?test=1` hook + `tools/dev-server.mjs` pattern as `full-slice.spec.mjs`): (a) **network-trace** — zero non-same-origin requests + zero `FORBIDDEN_DOMAINS` hits (the existing `network-trace.spec.mjs`/`full-slice.spec.mjs` contract, re-exercised here over the consolidated path including at least one methodology corpus page); (b) **CSP-violation count** — a `page.on('pageerror')` (and `page.on('console')` for `Content-Security-Policy` violation lines) collector asserts **zero** CSP violations across the full happy-path including methodology corpus pages; (c) **viewport-overflow** — iterates widths **320 / 375 / 414 / 768 / 1024 / 1280 / 1440** and asserts no horizontal scroll (`document.documentElement.scrollWidth <= clientWidth`, within a 1px tolerance) on every rendered surface (NFR1 implication). The spec is gated in `pr-checks.yml` as the `trust-verification-full` job and is authored to run **<90s end-to-end on CI** (seeded test-hook session, not physical click-through — same speed discipline as `full-slice.spec.mjs`). *Dev-phase:* file exists + is well-formed + asserts the three legs and the seven widths; passing a live 90s run is a CI gate, not a dev gate.

**AC-2 (`trust-verification-full` job ADDED + active + per-spec wired):** `.github/workflows/pr-checks.yml` declares a NEW top-level `trust-verification-full:` job (there is no Epic-1 stub to flip — see the reconciliation table) that is **active** (no `if: false`) and runs `tests/playwright/trust-verification.spec.mjs` explicitly via its own job, mirroring the existing per-spec Playwright jobs (chromium install + `make build-methodology` so corpus pages exist + `npx playwright test tests/playwright/trust-verification.spec.mjs`). Per lesson-2026-06-03-001, the wiring is an explicit per-spec job — NOT a greedy `tests/playwright/*.spec.mjs` glob; absence of the spec filename in pr-checks.yml == uncovered.

**AC-3 (Lighthouse perf budgets — `lighthouserc.json` populated + `lhci autorun` wired):** `tests/perf/lighthouserc.json` is populated (it is currently absent — `tests/perf/` holds only `.gitkeep`) with `ci.assert` assertions enforcing **FCP < 1.5s, LCP < 2.5s, TTI < 3.0s, CLS < 0.05** on a **mid-tier Android Slow-4G** emulation profile (`ci.collect.settings` with mobile form-factor + Slow-4G throttling). `pr-checks.yml` runs `npx --yes lhci autorun` against it (config path resolved per `@lhci/cli` convention — `lighthouserc.json` at `tests/perf/` referenced via `--config` or `LHCI_*`/`lhci.json` discovery; the engineer picks the lhci-correct wiring). This is wired into the existing **already-active** `lighthouse` job as an additional perf-budget leg (or a clearly-named sibling step), NOT a new job and NOT an `if:` flip — the existing accessibility-audit legs (light/dark, ≥0.90, Story 6.4) are preserved untouched. *Dev-phase:* `lighthouserc.json` exists + parses + carries the four budgets at the documented thresholds + the Slow-4G/mobile profile; the live `lhci autorun` runs in CI only.

**AC-4 (axe-core/pa11y — `tests/a11y/*.spec.mjs` authored + `axe-core-pa11y` activated):** `tests/a11y/` (currently `.gitkeep` only) gains at least one `*.spec.mjs` that runs **axe-core** against every methodology page **and** every SPA surface (landing, consent, item-runner, result, tail-scenes), asserting zero contrast / keyboard / landmark / aria violations; **pa11y** is documented as the fallback runner. The deferred `axe-core-pa11y` job in `pr-checks.yml` (currently `if: false`, "Activates in Epic 6") is **ACTIVATED** (remove `if: false`, wire the real per-spec run: chromium install + `make build-methodology` + `npx playwright test tests/a11y/<name>.spec.mjs`), per-spec (lesson-2026-06-03-001). *Dev-phase:* a11y spec file(s) exist + assert axe against the enumerated surfaces; the live full-page-set audit runs in CI.

**AC-5 (`csp-violation-count` + `viewport-overflow` jobs activated):** the two deferred jobs `csp-violation-count` (line ~270, `if: false`, "Activates in Epic 3") and `viewport-overflow` (line ~369, `if: false`, "Activates in Epic 6") in `pr-checks.yml` are **ACTIVATED** (remove `if: false`, wire a real run). Each may either (a) point at the consolidated `trust-verification.spec.mjs` (its CSP-violation / viewport-overflow legs), or (b) point at a dedicated spec — the engineer chooses, but the activated job MUST run a real Playwright assertion (not an `echo` stub) and be wired per-spec. Both jobs move into the active set in the frozen ci-matrix (AC-7).

**AC-6 (cropping-fuzzer confirmed in consolidated suite — no flip needed):** `cropping-fuzzer` is **already active** (Story 6.6) and self-gates on `corpus/manifest.json` `bankFrozen`, which is **already `true`** — so it already runs on every PR forward. This story makes NO activation change to it; it (a) confirms the active+frozen state via the structural test, and (b) ensures the consolidated trust-verification narrative/suite references the fuzzer's pass/fail (e.g. the new structural test asserts `cropping-fuzzer` is present + active + bankFrozen-gated). No code edit to `cropping-fuzzer.spec.mjs` is required.

**AC-7 (graduate frozen class-A `ci-matrix.test.mjs` + NEW structural suite test + integrity):** `tests/scaffold/ci-matrix.test.mjs` is `artefact_class: A`. This story (a) **ADDS** `trust-verification-full` to the `ALL_JOBS` array, (b) **MOVES** `csp-violation-count`, `viewport-overflow`, `axe-core-pa11y` AND adds `trust-verification-full` into the `EPIC_1_ACTIVE` set (so AC-2 stops asserting `if: false` on them and AC-3 stops requiring it), (c) leaves `lighthouse` + `cropping-fuzzer` where they already are (already in `EPIC_1_ACTIVE`). A NEW `tests/scaffold/trust-verification-suite.test.mjs` asserts the structural contract: `trust-verification.spec.mjs` exists + its source contains the network-trace + CSP (`pageerror`) + the seven viewport-width literals; `tests/perf/lighthouserc.json` exists + parses + carries the four budgets (FCP/LCP/TTI/CLS) + the Slow-4G/mobile profile; at least one `tests/a11y/*.spec.mjs` exists + references axe; and `pr-checks.yml` carries the active (`if: false`-free) `trust-verification-full`, `csp-violation-count`, `viewport-overflow`, `axe-core-pa11y`, `lighthouse` (with the lhci leg) jobs, each per-spec wired. After editing the class-A test, **re-register integrity** via `tds integrity record --files=tests/scaffold/ci-matrix.test.mjs` (engineer) and re-grep `state-manifest.yaml` after the next sweep to confirm the new hash persisted (lesson-2026-05-20-007 / lesson-2026-05-19-013).

**AC-8 (regression gate — no green-on-broken-frozen-set):** `make test` green, `make lint` green, `make build` deterministic. Crucially, the ci-matrix `EPIC_1_ACTIVE`/`ALL_JOBS` edit and the pr-checks.yml activations MUST land together so AC-2 (active jobs carry no `if: false`) and AC-3 (deferred jobs carry `if: false`) both stay green — the lesson-2026-06-03-002 failure mode (activate a job, forget the frozen job-set → self-inflicted red, then misattribute it as pre-existing). Before labeling any red test pre-existing, run a baseline diff (`git diff main -- <file>` or a stash-and-rerun) and quote it in the self-review.

## Tasks / Subtasks

- [ ] **Task 1: Author the consolidated trust-verification spec** (AC: 1)
  - [ ] Create `tests/playwright/trust-verification.spec.mjs` driving the full happy-path via the `window.__IQME_TEST__` `?test=1` hook + `tools/dev-server.mjs` (mirror `full-slice.spec.mjs` setup/teardown + seeded 16-item session, NOT physical click-through, for the <90s budget).
  - [ ] Leg A network-trace: zero non-same-origin requests + zero `FORBIDDEN_DOMAINS` hits across the path incl >=1 methodology page (reuse the `full-slice.spec.mjs` FORBIDDEN_DOMAINS list).
  - [ ] Leg B CSP-violation count: `page.on('pageerror')` + console-CSP collector → assert zero CSP violations across the happy-path incl methodology pages.
  - [ ] Leg C viewport-overflow: iterate widths 320/375/414/768/1024/1280/1440, assert `scrollWidth <= clientWidth` (1px tolerance) on each rendered surface.
- [ ] **Task 2: Add the trust-verification-full job to pr-checks.yml** (AC: 2)
  - [ ] Declare a NEW active `trust-verification-full:` job (no `if: false`) per-spec wired (chromium install + `make build-methodology` + `npx playwright test tests/playwright/trust-verification.spec.mjs`); mirror the existing Playwright job shape. Verify via grep that the spec filename appears in pr-checks.yml (lesson-2026-06-03-001).
- [ ] **Task 3: Populate lighthouserc.json + wire lhci autorun** (AC: 3)
  - [ ] Create `tests/perf/lighthouserc.json` with `ci.assert` for FCP<1.5s, LCP<2.5s, TTI<3.0s, CLS<0.05 + `ci.collect.settings` mid-tier-Android Slow-4G mobile profile.
  - [ ] Wire `npx --yes lhci autorun` (config at `tests/perf/lighthouserc.json`) into the EXISTING already-active `lighthouse` job as a perf-budget leg; preserve the Story-6.4 light/dark accessibility legs untouched. Do NOT flip an `if:` (there is none) and do NOT add a new job.
- [ ] **Task 4: Author a11y specs + activate axe-core-pa11y** (AC: 4)
  - [ ] Create `tests/a11y/<name>.spec.mjs` running axe-core against every methodology page + every SPA surface (landing/consent/item-runner/result/tail-scenes); document pa11y fallback.
  - [ ] Activate the `axe-core-pa11y` job in pr-checks.yml: remove `if: false`, wire the real per-spec run (chromium install + `make build-methodology` + `npx playwright test tests/a11y/<name>.spec.mjs`). Verify spec filename present in pr-checks.yml.
- [ ] **Task 5: Activate csp-violation-count + viewport-overflow jobs** (AC: 5)
  - [ ] Remove `if: false` from both deferred jobs; wire each to a real Playwright run (either the consolidated `trust-verification.spec.mjs` legs or dedicated specs — engineer's call), per-spec, no echo stub.
- [ ] **Task 6: Confirm cropping-fuzzer forward-running** (AC: 6)
  - [ ] No edit to `cropping-fuzzer.spec.mjs`; confirm it is active + self-gates on `bankFrozen: true` (already true) and is asserted present+active by the new structural test.
- [ ] **Task 7: Graduate ci-matrix + add structural suite test + re-register integrity** (AC: 7) — TEST-AUTHOR PHASE, class-A frozen edit
  - [ ] Edit `tests/scaffold/ci-matrix.test.mjs`: add `trust-verification-full` to `ALL_JOBS`; add `trust-verification-full`, `csp-violation-count`, `viewport-overflow`, `axe-core-pa11y` to the `EPIC_1_ACTIVE` set (leave `lighthouse` + `cropping-fuzzer` as-is — already active).
  - [ ] Create `tests/scaffold/trust-verification-suite.test.mjs` (structural: trust-verification.spec.mjs exists + has network-trace/pageerror-CSP/seven viewport widths; lighthouserc.json parses + has the 4 budgets + Slow-4G profile; >=1 tests/a11y/*.spec.mjs exists + references axe; pr-checks.yml has the active per-spec jobs).
  - [ ] `tds integrity record --files=tests/scaffold/ci-matrix.test.mjs --reason="story-8-5: add trust-verification-full + graduate csp/viewport/axe deferred→active"` as engineer; re-grep `state-manifest.yaml` after the next state-commit sweep to confirm the new hash persisted (do NOT hand-edit state-manifest.yaml).
- [ ] **Task 8: Regression gate + provenance discipline** (AC: 8)
  - [ ] `make test` / `make lint` green; `make build` deterministic. Land the ci-matrix graduation + pr-checks activations together so AC-2/AC-3 both stay green. Baseline-diff (`git diff main -- <file>`) any ambiguous failure before labeling it pre-existing (lesson-2026-06-03-002).

## Dev Notes

- **Dev-phase contract is STRUCTURAL (re-stated, load-bearing).** The frozen tests assert files exist + are well-formed + assert the right things + pr-checks jobs are active and per-spec-wired. They do NOT require a green live 90s Playwright run, a live `lhci autorun`, or a live full-page axe audit — those run in CI where chromium + the Slow-4G throttle profile exist. Do NOT author a frozen test that fails in dev merely because chromium is unavailable or the live run exceeds a time budget. (Same posture as Story 8.1.)
- **The job-status reconciliation table in the dev-phase note above is authoritative.** Read pr-checks.yml before editing. `lighthouse` and `cropping-fuzzer` are ALREADY ACTIVE — do not "activate" them; `lighthouse` needs the lhci perf-budget leg ADDED (its existing accessibility legs stay). The genuinely-deferred (`if: false`) jobs are exactly `csp-violation-count`, `viewport-overflow`, `axe-core-pa11y`. `trust-verification-full` does NOT exist and must be ADDED (new job + new ALL_JOBS/EPIC_1_ACTIVE entry).
- **Per-spec CI wiring, never a glob (lesson-2026-06-03-001 — CRITICAL for this story).** pr-checks.yml lists Playwright specs one job per spec; there is NO `tests/playwright/*.spec.mjs` glob run. `trust-verification.spec.mjs` and each `tests/a11y/*.spec.mjs` need an explicit job (or explicit `npx playwright test <path>` step). Verification step is non-negotiable: `grep trust-verification.spec.mjs .github/workflows/pr-checks.yml` (and the a11y spec filename) must return a hit before Task 2/Task 4 are marked done. Absence == uncovered in CI.
- **SPA driving + speed (mirror full-slice.spec.mjs).** Use `import { start } from "../../tools/dev-server.mjs"` + `beforeAll`/`afterAll`; `page.goto(\`${origin}/?test=1\`)`; `await page.waitForFunction(() => window.__IQME_TEST__ !== undefined)`; `resetState()` → `setSeed(...)` → 16× `recordResponse(i, ...)` → `navigate("result")`. This avoids the 30s+ physical click-through and Playwright races, keeping the consolidated spec under the 90s CI budget.
- **Methodology pages must exist for the corpus-page legs.** The CSP/network legs touch >=1 methodology corpus page (e.g. `/methodology/v0.1.0/en/scoring/percentile-to-iq/`), so the job step runs `make build-methodology` before the Playwright run (same prerequisite as `full-slice` / `co-equal-triplet-computed-style` jobs). The a11y "every methodology page" leg also needs the built corpus.
- **Lighthouse: ADD a perf leg, don't replace.** The existing `lighthouse` job (pr-checks.yml ~line 428) runs `lighthouse@12 --only-categories=accessibility` ≥0.90 on a light+dark matrix (Story 6.4, NFR12 contrast). Story 8.5 ADDS the `lhci autorun tests/perf/lighthouserc.json` perf-budget enforcement (NFR1: FCP/LCP/TTI/CLS on mid-tier Android Slow-4G). Keep both — accessibility AND perf. `@lhci/cli` discovers `lighthouserc.json`/`.lighthouserc.json` or takes `--config`; the engineer picks the lhci-correct invocation pointing at `tests/perf/lighthouserc.json`.
- **cropping-fuzzer self-gate is already satisfied.** `tests/playwright/cropping-fuzzer.spec.mjs` does `test.skip(!bankFrozen(), ...)` reading `corpus/manifest.json` `bankFrozen`, which is `true` in-repo. So the fuzzer already runs live on every PR. AC-6 is a confirmation + a structural assertion, not a code edit.
- **CLASS-A frozen edit (AC-7) — non-optional ceremony.** `tests/scaffold/ci-matrix.test.mjs` is `artefact_class: A`. Adding `trust-verification-full` and moving `csp-violation-count`/`viewport-overflow`/`axe-core-pa11y` into `EPIC_1_ACTIVE` is an authorized in-scope graduation (this story OWNS the activations). After editing, run `tds integrity record --files=tests/scaffold/ci-matrix.test.mjs` as **engineer**, then re-grep `state-manifest.yaml` after the next sweep to confirm the new hash didn't resurrect the old one (lesson-2026-05-19-013). Do NOT hand-edit state-manifest.yaml.
- **Why the activate-and-update-the-frozen-set-together discipline matters (lesson-2026-06-03-002).** Epic-6 activated CI jobs WITHOUT updating the frozen `EPIC_1_ACTIVE` set, turning `ci-matrix.test.mjs` AC-3 red, then misattributed the red as pre-existing. This story touches the SAME surface — land the pr-checks activations and the ci-matrix graduation in one coherent change; verify AC-2 (active → no `if: false`) and AC-3 (deferred → has `if: false`) both green after the edit.
- **`trust-verification-suite.test.mjs` is a scaffold test, not a Playwright spec.** It lives in `tests/scaffold/` → runs under `make test` (node --test, structural text/JSON assertions like `ci-matrix.test.mjs`), so it needs NO per-spec pr-checks job (lesson-2026-06-03-001 applies to Playwright specs; this is in the batched scaffold run).
- **Files (engineer phase):** `tests/playwright/trust-verification.spec.mjs` (NEW), `tests/perf/lighthouserc.json` (NEW), `tests/a11y/<name>.spec.mjs` (NEW), `.github/workflows/pr-checks.yml` (add `trust-verification-full`; add lhci leg to `lighthouse`; remove `if: false` from `csp-violation-count` + `viewport-overflow` + `axe-core-pa11y`). **Test-author phase:** `tests/scaffold/ci-matrix.test.mjs` (class-A graduation), `tests/scaffold/trust-verification-suite.test.mjs` (NEW structural).

### Carry-forward lessons

<untrusted-content source="lessons.yaml" lesson_id="lesson-2026-06-03-001" severity="high">
summary: pr-checks.yml wires Playwright specs per-spec (one job each), NOT via a greedy glob; a new spec is uncovered in CI until explicitly added
preferred_pattern: Add a dedicated per-spec job to .github/workflows/pr-checks.yml mirroring existing jobs; verify with: grep <spec-filename> .github/workflows/pr-checks.yml before marking the wiring task done
</untrusted-content>

- **lesson-2026-06-03-001 (high) — CRITICAL FOR THIS STORY.** pr-checks.yml has NO greedy glob; each Playwright spec needs its own job. Apply: `trust-verification.spec.mjs` gets the new `trust-verification-full` job; each `tests/a11y/*.spec.mjs` gets wired into the `axe-core-pa11y` job (explicit `npx playwright test <path>`). VERIFY by grepping the spec filename in pr-checks.yml before marking Task 2/Task 4 done. Epic-6 shipped two uncovered specs on the false "it globs the directory" premise — do not repeat.

- **lesson-2026-05-20-007 (high) — class-A frozen edit ceremony.** Stories touching class-A frozen tests must carry this Carry-forward section AND re-register integrity. Apply: graduating `ci-matrix.test.mjs` (`ALL_JOBS` + `EPIC_1_ACTIVE`) is a class-A edit → `tds integrity record` it (Task 7); the section you are reading is the mandatory injection.

- **lesson-2026-06-03-002 (high) — provenance via baseline diff; activate-with-frozen-set-together.** Epic-6 activated CI jobs without updating the frozen job-set (self-inflicted red on ci-matrix AC-3) then misattributed provenance in both directions. Apply: land the pr-checks.yml activations and the ci-matrix `EPIC_1_ACTIVE` graduation together (Task 7+8); before labeling any red test pre-existing vs introduced, quote a `git diff main -- <file>` (or stash-and-rerun) in the self-review.

- **lesson-2026-05-19-013 (high) — verify integrity persists after sweep.** Direct state-manifest.yaml edits can be silently undone by the next `tds state-commit` sweep. Apply: after `tds integrity record` for the edited ci-matrix, re-grep `state-manifest.yaml` post-sweep to confirm the new hash persisted; never hand-edit state-manifest.yaml.

- lesson-2026-06-04-002 (high) — informational, not directly applicable here (harm-critical crisis-hotline placeholders must not fabricate `tel:` specifics). The a11y spec touches tail-scene crisis-resource surfaces structurally (landmark/aria/contrast only) — it asserts accessibility, not content correctness, so it does not introduce or validate hotline specifics. No action beyond awareness.

### Project Structure Notes

- Playwright specs under `tests/playwright/`; perf config under `tests/perf/`; a11y specs under `tests/a11y/`; scaffold (node --test) tests under `tests/scaffold/`; CI workflows under `.github/workflows/`. All within Domain D (`tools/` + `.github/workflows/` + `Makefile` + test surfaces). No structural variance.
- `tests/perf/` and `tests/a11y/` currently hold only `.gitkeep` — this story populates them (first real files in each). `tests/playwright/trust-verification.spec.mjs` is net-new alongside the existing 12 specs.
- Optional: a `make test-trust-verification` / `make test-a11y` target may be added to the Makefile mirroring `test-full-slice` (build-methodology prerequisite + `npx playwright test <path>`) for local convenience; not required by any AC. The Playwright specs remain excluded from the default `make test` batch (which is node --test only).

### References

- [Source: epics.md#Story-8.5] (~lines 2017–2043) — AC source: consolidated trust-verification.spec.mjs (network-trace + CSP-violation via `page.on('pageerror')` + viewport-overflow 320–1440), `trust-verification-full` job, <90s CI; Lighthouse `lighthouserc.json` + `lhci autorun` FCP<1.5/LCP<2.5/TTI<3.0/CLS<0.05 mid-tier Android Slow-4G; axe-core/pa11y against every methodology page + every SPA surface; cropping-fuzzer gated on `bankFrozen`.
- [Source: .github/workflows/pr-checks.yml] — current job set: `network-trace`/`full-slice-network-trace` active; `lighthouse` (~428) ALREADY ACTIVE (accessibility light/dark); `cropping-fuzzer` (~410) ALREADY ACTIVE (bankFrozen self-gate); `csp-violation-count` (~270), `viewport-overflow` (~369), `axe-core-pa11y` (~469) DEFERRED `if: false`; `trust-verification-full` ABSENT. Header "no new jobs after Epic 1" discipline.
- [Source: tests/scaffold/ci-matrix.test.mjs] — `ALL_JOBS` array (~22–60) + `EPIC_1_ACTIVE` set (~62–126); AC-1 (all jobs present), AC-2 (active → no `if: false`), AC-3 (deferred → `if: false` + Activates-in comment). `trust-verification-full` absent from `ALL_JOBS`. Class-A frozen.
- [Source: tests/playwright/full-slice.spec.mjs] — SPA happy-path driving template (dev-server `start`, `?test=1`, `__IQME_TEST__` seeded 16-item session, FORBIDDEN_DOMAINS, networkidle, scoped assertions). Mirror for `trust-verification.spec.mjs`.
- [Source: tests/playwright/network-trace.spec.mjs] — FORBIDDEN_DOMAINS list + same-origin-only network assertion contract.
- [Source: tests/playwright/cropping-fuzzer.spec.mjs] — `bankFrozen()` self-gate pattern; `corpus/manifest.json` `bankFrozen: true` confirmed.
- [Source: corpus/manifest.json] — `bankFrozen: true` (cropping-fuzzer runs live forward; AC-6).
- [Source: Makefile] — `test` (node --test, Playwright excluded), `test-full-slice` (build-methodology + playwright), `build-methodology`, `dev` (`tools/dev-server.mjs` on :4173).
- [Source: _bmad-output/implementation-artifacts/stories/8-1-release-yml-workflow-app-v-corpus-v-tag-triggers-deployment.md] — infra-now structural-contract posture + class-A ci-matrix graduation + integrity ceremony precedent.

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## Specialist Self-Review
