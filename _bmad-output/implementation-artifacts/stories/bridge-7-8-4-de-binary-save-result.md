---
id: bridge-7-8-4-de-binary-save-result
title: "Story bridge-7-8-4-de-binary-save-result: De-binary save-result.test.mjs: replace literal control bytes with \\x escapes"
status: review
---

# Story bridge-7-8-4-de-binary-save-result: De-binary save-result.test.mjs: replace literal control bytes with \x escapes

## Story

tests/unit/save-result.test.mjs embeds literal 0x00 and 0x1F bytes in the hashSeed no-control-chars regex, so git classifies the 9.5KB file as binary and its test logic is invisible to diffs, grep, and PR-review tooling. The test runs 5/5 green and integrity is intact, so this is not a functional defect — it is a reviewability defect that persists in every future PR until fixed. Small, concrete, no behaviour change: swap the two literal bytes for the textual \x00 / \x1f escape forms, re-record class-A integrity for the file. Still UNRESOLVED at epic-6 close (file(1) reports "data"). Pairs with lesson-2026-06-03-003 which prevents recurrence; this bridge fixes the existing instance.

## Sources (deferred from)

- `6-7-opt-in-localstorage-save-retest-effect-copy-on-result-page` (kind=auditor-finding, round=3, finding_index=1)

## Dev Notes

### Carry-forward lessons

- lesson-2026-05-19-001 — frozen-test edit ratified via the unfreeze-window + `tds integrity record` ceremony (targeted the owning story 6-7).
- lesson-2026-06-03-002 — provenance: confirmed byte-identical regex behavior (5/5 green) before relabeling the change cosmetic.

## Acceptance Criteria

1. `tests/unit/save-result.test.mjs` contains **no literal control bytes**; the hashSeed no-control-chars regex (≈ line 130) uses the textual escape forms `\x00` / `\x1f` instead of raw 0x00 / 0x1F bytes.
2. `file tests/unit/save-result.test.mjs` reports a **text** type (not `data`); the file diffs, greps, and reviews as normal UTF-8 text.
3. Test behavior is **byte-identical**: `node --test tests/unit/save-result.test.mjs` runs green (same 5/5 assertions) — the no-control-chars assertion still holds.
4. **Class-A integrity re-recorded** for the file after the edit (via the unfreeze-window + `tds integrity record` ceremony), so `state-manifest.yaml` reflects the new content. No other files change.

## Tasks / Subtasks

- [x] Task 1 — Open an unfreeze window for `tests/unit/save-result.test.mjs` (class-A frozen): `tds story unfreeze-tests --files=tests/unit/save-result.test.mjs` under this bridge story.
- [x] Task 2 — Replace the two literal control bytes in the regex (≈ line 130) with `\x00` / `\x1f` escape sequences; confirm `file(1)` now reports text and the file diffs as UTF-8 (AC 1, 2).
- [x] Task 3 — Run `node --test tests/unit/save-result.test.mjs`; confirm green / behavior unchanged (AC 3).
- [x] Task 4 — Re-record class-A integrity (`tds integrity record`) and close the unfreeze window (AC 4).

## Dev Agent Record

### Completion Notes List

- De-binarized tests/unit/save-result.test.mjs: replaced literal 0x00/0x1F bytes in line-130 no-control-chars regex with textual \x00/\x1f escapes (AC1). file(1) now reports UTF-8 text not data (AC2). node --test 5/5 green, byte-identical behavior (AC3). Frozen-test ceremony against owning story 6-7: unfreeze-tests → edit → integrity record (closed window); integrity verify 163/0, no other files changed (AC4).

### File List

---
Generated by `tds epic create-bridge-from-retros` for bridge `bridge-7-8` (blocks `epic-8`).
- tests/unit/save-result.test.mjs

## Specialist Self-Review

**Decisions made:**
- Replaced the two literal control bytes (0x00, 0x1F) in the line-130 no-control-chars regex with their textual escape forms `\x00` / `\x1f`. The regex `[\x00-\x1f]` is byte-equivalent to the former literal-byte character class, so the assertion semantics are unchanged.
- Performed the byte→escape swap with a scoped `node` replace (`/\x00/g`, `/\x1f/g`) rather than a hand-typed Edit, since the literal bytes are non-printable and only line 130 carried them (verified by a pre-scan: exactly 2 control bytes, both on line 130).
- Ran the full frozen-test-edit ceremony against the **owning** story (6-7) — `tds story unfreeze-tests --story=6-7-… --files=… --as=engineer` → edit → `tds integrity record --as=engineer --story=6-7-… --files=…`. The file is 6-7's artifact; ownership and the integrity record stay with 6-7, bridge-7-8-4 is the work context (captured in the unfreeze/record reason).

**Alternatives considered:**
- Unfreezing under bridge-7-8-4 (transferring ownership) — rejected: the CLI requires `--story` to match the owning record (6-7), and transferring ownership of a settled epic-6 artifact to a bridge story would muddy provenance. Keep it with 6-7.

**Framework gotchas avoided:**
- 6-7 is `done`; the unfreeze auto-flip only fires from `{tests-approved, tests-drafting}`, so opening the window did **not** corrupt 6-7's status (verified `done` before and after).
- `file(1)` now reports "Java source, Unicode text, UTF-8 text" — the "Java source" label is file(1) heuristic noise for `.mjs`; the load-bearing part is **UTF-8 text** (no longer `data`), so the file diffs/greps/reviews normally (AC2).

**Areas of uncertainty:**
- None material. The change is a no-op on runtime behavior (regex character class is byte-identical); the only observable deltas are git's text/binary classification and the file's recorded sha256.

**Tested edge cases:**
- `node --test tests/unit/save-result.test.mjs` → 5/5 green, including the no-control-chars assertion itself (AC3 — byte-identical behavior).
- `tds integrity verify` → 163/0; unfreeze-windows.yaml back to `windows: []` (window closed). No other files changed (AC4).
