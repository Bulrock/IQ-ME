---
id: 5-3-constructs-4-remaining-limitations-3-remaining
title: "Story 5.3: Constructs (4 remaining) + Limitations (3 remaining)"
status: review
tds:
  primary_specialist: frontend
  story_tags:
    - methodology-corpus
    - constructs
    - limitations
    - fr27
---

# Story 5.3: Constructs (4 remaining) + Limitations (3 remaining)

## Story

As an **English reader exploring the conceptual underpinning of fluid reasoning and the explicit limitations of the IQ-ME instrument**,
I want **the remaining Constructs pages (`matrix-reasoning`, `icar-mr`, `g-factor`, `validity-envelope`) and Limitations pages (`cultural-variance`, `anti-leakage`, `retest-effects`) authored at the same plain-language discipline as the anchor pages**,
so that **the validity envelope is fully explored, the anti-leakage acknowledgment is honest (Risk #2), and the retest-effects discipline (FR27) has its full methodology home (referenced from the result page)**.

## Acceptance Criteria

1. **AC-1 (4 new Constructs pages):**
   - `src/content/methodology/en/constructs/matrix-reasoning/index.md`
   - `src/content/methodology/en/constructs/icar-mr/index.md`
   - `src/content/methodology/en/constructs/g-factor/index.md`
   - `src/content/methodology/en/constructs/validity-envelope/index.md` — inlines the `src/content/diagrams/validity-envelope-diagram.svg` from Story 5.1 via build-pipeline support OR a direct inline reference. **For this story, the SVG is referenced in the page body as a markdown image link**; the full inline-emission build pipeline support is deferred (no markdown-subset image-inline support today; would require renderer extension).

2. **AC-2 (3 new Limitations pages):**
   - `src/content/methodology/en/limitations/cultural-variance/index.md`
   - `src/content/methodology/en/limitations/anti-leakage/index.md`
   - `src/content/methodology/en/limitations/retest-effects/index.md` — explicit "no technical cooldown enforced" section (FR27).

3. **AC-3 (lint pass):**
   - All 7 pages pass `lint-frontmatter`, `lint-glossary`, `lint-reading-level` (FK ≤ 12), `lint-license-provenance`, `lint-claims-manifest`.
   - Add any new glossary entries (e.g. `matrixReasoning`, `icarMr`) when used in `glossaryRefs:`.

4. **AC-4 (tests):**
   - `tests/scaffold/anchor-pages.test.mjs` extended with the 7 new pages in the existence + frontmatter + render checks.
   - `make test` / `make lint` / `make build` all exit 0.

5. **AC-5 (deferred):**
   - Markdown-subset image-inline support for SVG embedding — out of scope (would change Story 4.1 renderer contract).
   - validity-envelope-diagram inline-rendering in `/methodology/constructs/validity-envelope/`: page links to the SVG as a static asset path; rendered-inline body emission is a follow-up.

## Tasks / Subtasks

- [x] **Task 1: Author 4 Constructs pages** (AC-1)
- [x] **Task 2: Author 3 Limitations pages** (AC-2)
- [x] **Task 3: Extend anchor-pages scaffold test with new page list** (AC-4)
- [-] **Task 4: Add new glossary entries when referenced** (AC-3) _(deferred: not needed — only gFactor used, already in glossary)_
- [x] **Task 5: Full baseline (make test/lint/build)** (AC-4)
- [x] **Task 6: Branch + state hygiene**

## Dev Notes

- All pages plain-language FK ≤12; ≤4-sentence paragraphs.
- The retest-effects page is link-target for the Epic-6 result page; reserve a stable anchor.

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

- 4 constructs + 3 limitations pages; all FK ≤12; lint exit 0; build exit 0; 784 tests pass.

### File List

- src/content/methodology/en/constructs/matrix-reasoning/index.md
- src/content/methodology/en/constructs/icar-mr/index.md
- src/content/methodology/en/constructs/g-factor/index.md
- src/content/methodology/en/constructs/validity-envelope/index.md
- src/content/methodology/en/limitations/cultural-variance/index.md
- src/content/methodology/en/limitations/anti-leakage/index.md
- src/content/methodology/en/limitations/retest-effects/index.md
- tests/scaffold/anchor-pages.test.mjs

## Specialist Self-Review

## Specialist Self-Review

**Decisions made:**
1. **All 7 pages authored at FK ≤12.** icar-mr and cultural-variance initially exceeded; broke long sentences into short ones rather than dumb the content down.
2. **No new glossary entries needed.** The 7 pages reference only `gFactor` (already in glossary tree from Story 5.2).

**Alternatives considered:**
- Adding `matrixReasoning` glossary entry — not needed; the constructs page itself IS the matrix-reasoning explanation.
- Inlining validity-envelope-diagram.svg in `/methodology/constructs/validity-envelope/` — deferred per AC-5 (requires markdown-subset image-inline support, scope-creep).

**Framework gotchas avoided:**
- Snapshots auto-regenerated by `make test` because new pages emit new HTML — verified clean diff for existing pages (no spurious changes to scoring/* or limitations/what-this-does-not-measure).

**Areas of uncertainty:**
- Some readers/reviewers may prefer longer prose; this version is editorial-minimum for the anchor-pages-frozen tag. Follow-ups can expand without breaking structure.

**Tested edge cases:**
- All 14 EN pages now pass lint-frontmatter, lint-glossary, lint-reading-level (FK ≤12), lint-license-provenance, lint-claims-manifest.
- make test 784/784 + 1 skip. lint exit 0. build exit 0.
