# ICAR license-confirmation outreach log (Gate 9a)

> **STATUS: PENDING — not yet sent, no reply has arrived.** This is a
> launch-readiness tracking log for pre-launch gate #1 (ICAR license confirmation
> — Risk #1, existential). The drafted message lives in
> [`icar-outreach-draft.md`](icar-outreach-draft.md). Until a real written reply is
> committed as `ICAR-CONFIRMATION.pdf`, the gate is **open**.

## Gate summary

- **Gate:** 9a — ICAR License Confirmation
- **Artifact of record:** `ICAR-CONFIRMATION.pdf` at repo root (currently the
  Epic-1 / FR45 visible-fallback placeholder slot — **not** yet the real reply)
- **Risk:** #1 (existential) — redistribution of the ICAR-MR item pool depends on
  this confirmation
- **Latency expectation:** high / unbounded (academic email response times)

## Outreach attempts

_All rows are **pending** until a real send happens. Do not pre-fill dates or
outcomes — record only what actually occurred (lesson-2026-06-04-002: never
fabricate specifics)._

| # | Date sent | Channel | Recipient | Outcome | Notes |
|---|-----------|---------|-----------|---------|-------|
| 1 | pending | pending | pending (TBD per draft) | pending | initial contact — draft ready, not yet sent |

## Fallback of record

If ICAR confirmation never lands (or returns a constraint making redistribution
unsuitable), the documented fallback is to **launch with the explicitly-public
OpenPsychometrics-licensed item subset** (a smaller item pool, also documented in
the methodology corpus). This keeps launch unblocked without presuming a
permission that was never granted.

## Hand-off to Story 9a-2

When a written reply arrives, **Story 9a-2** owns: committing the reply as
`ICAR-CONFIRMATION.pdf` (replacing the placeholder slot), reconciling `LICENSES.md`
with any noted constraints, and updating this log's attempts table with the real
date/outcome. Only then is Gate 9a documented closed (and reflected in
`CHANGELOG.md` per the launch-readiness discipline).
