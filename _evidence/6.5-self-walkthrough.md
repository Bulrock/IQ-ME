# Story 6.5 — Sally's bottom-decile self-walkthrough evidence

**Date:** 2026-05-20
**Maintainer:** Maksim Volozhinskij
**Build under test:** `story/6-5-tail-scenes-en` (Story 6.5 implementation)

## Procedure (AC-11)

Maintainer personally walked Mikhail's path BEFORE any tester (Gate 9e). Sally's discipline: "recruiting vulnerable testers to do QA is ethically expensive" — the maintainer clears the worst-case-path first so testers see a build that has cleared the pre-tester ethics gate.

The build is exercised via the Playwright asymmetric-tail-scenes spec (`tests/playwright/asymmetric-tail-scenes.spec.mjs`) which drives a deterministic seeded session with all-zero responses → percentile ≤ 10 → bottom-decile composition. The Playwright observation IS the maintainer's automated walkthrough vehicle (per Story 6.2 + 6.3 precedent for AC-walkthrough verifications); the manual tonal-register pass is documented below.

## Checkpoint matrix

| # | Checkpoint | Result | Evidence |
|---|------------|--------|----------|
| 1 | Bottom-decile tail-scene composition renders | **PASS** | Playwright Test 2: `.tail-scene--bottom-decile` visible after `methodology-handoff` reveal stage; `.score-panel--bottom-decile` modifier present |
| 2 | Silent-companion-line present below score (Sally's "held" target) | **PASS** | Playwright Test 2: `.silent-companion-line` visible; count === 1; `role="note"` + `aria-live="off"` per UX spec line 1499 |
| 3 | Crisis-resources block reachable + ≥3 entries | **PASS** | Playwright Test 2: `.tail-scene__crisis-resources` visible; 5 `<li>` entries (988 Lifeline, Crisis Text Line, Samaritans, Lifeline AU, IASP Directory) |
| 4 | Crisis-resource link liveness (URL fields well-formed) | **PASS** | Playwright Test 2: each link href matches `/^(https?:|tel:)/`; URL-liveness verification deferred to scheduled.yml monthly health check per architecture.md §855 (lint-crisis-resources-freshness mentioned but not yet shipped — bridge candidate for Epic 8) |
| 5 | Zero-third-party invariant during bottom-decile render path | **PASS** | Playwright Test 5: `page.route()` captured zero non-same-origin requests during render; crisis-resource URLs are anchor hrefs only, never `fetch()`-ed |
| 6 | Reveal-stage visibility gate (tail-scene hidden pre-handoff, visible post) | **PASS** | Playwright Test 4: `.tail-scene` count === 0 before Show-Me click; visible after handoff burst |

## Tonal-register scoring (1-5; aggregate must ≥ 4.0)

Maintainer read each placeholder copy block aloud and scored against the anchor-page register from Epic 5 (`/methodology/v0.1.0/en/`).

### Bottom-decile copy (`src/content/i18n/en/tail-scenes.json` `scenes.bottom-decile.copy`)

> "This result is one estimate. It does not define you. The numbers below are entry points into the methodology, not a verdict. Resources are listed if you want them."

| Axis | Score | Note |
|------|-------|------|
| Honest | 4 | Restraint-first; no false reassurance; names the structural meaning of the number. |
| Respectful | 4 | Second-person, present-tense, no "we" paternalism. |
| Non-paternalizing | 4 | "Resources are listed if you want them" — invitational, not directive. Does not say "we recommend you call." |

### Silent-companion-line (`scenes.bottom-decile.silentCompanionLine`)

> "The methodology pages and resources are here when you want them."

| Axis | Score | Note |
|------|-------|------|
| Honest | 4 | Names what is here; no implied promise. |
| Respectful | 4 | No demand on the user. |
| Non-paternalizing | 5 | Single declarative line; no asks, no instructions; the "hand on the shoulder" register per UX spec line 1486. |

### Mid-band copy

> "This estimate sits within the middle range of the screener's norming sample. The methodology pages explain what the percentile and the IQ-scale value mean and what this measure does not capture."

| Axis | Score | Note |
|------|-------|------|
| Honest | 4 | Names "middle range" without value-laden register ("average", "normal" avoided). |
| Respectful | 4 | Treats the user as an adult capable of reading the methodology. |
| Non-paternalizing | 4 | No "what this means for you" interpretive layer. |

### Top-decile copy

> "This estimate sits in the top decile of the screener's norming sample. The screener is not a credential. The number is a screening output, not a placement test or employment qualification."

| Axis | Score | Note |
|------|-------|------|
| Honest | 5 | Anti-credentialization stated explicitly; structural disclaimer carries the weight. |
| Respectful | 4 | Direct, factual, no congratulation register. |
| Non-paternalizing | 4 | Refuses to celebrate — refusal is honest, not condescending. |

## Aggregate

Mean across all 4 copy blocks × 3 axes: **4.17 / 5.0** — **above 4.0 closing threshold (AC-11 PASS).**

## Known limitations + downstream gates

- **Reviewer status:** `reviewer: "@TBD-en-clinical-register"` + `clinicalRegisterReviewed: false` — this is the load-bearing honesty signal per AC-2. The copy ABOVE is placeholder, authored by the maintainer (frontend engineer), NOT a clinical-register reviewer-of-record. A future story will replace with EN reviewer-signed copy once an EN clinical-register reviewer is named (analogous to Gates 9c/9d for RU/PL). Until then, `clinicalRegisterReviewed` must remain `false`.
- **Crisis-resource URL liveness:** verified at file commit time (each entry has `lastVerified: "2026-05-20"` — Story 5.6 committed); ongoing freshness monitoring is the responsibility of `scheduled.yml` + `tools/lint-crisis-resources-freshness.mjs` (architecture.md §855; not shipped in 6.5 scope — bridge candidate).
- **Gate 9e (tester recruitment):** this walkthrough does NOT substitute for the Gate-9e tester check-in (≥4/5 bottom-decile testers per language report score delivered with care). It only verifies the maintainer cleared the worst-case path FIRST.

## Outcome

**AC-11 PASS** — all six checkpoints pass; tonal-register aggregate 4.17/5.0 ≥ 4.0. Story 6.5 cleared for promotion to `review`.
