# 50-frameworks/ — Java techstack-pack (deferred)

> Status: **deferred to a post-1.0 expansion**.
> The Java role-skill references files in this directory by path
> (`50-frameworks/<framework>.md`) under "lazy-load on demand" semantics.
> They are not yet authored.

## Planned deep-dives

| File             | Topic                                                       |
|------------------|-------------------------------------------------------------|
| `spring.md`      | Boot 3.x lifecycle, AOT, virtual threads, profiles          |
| `hibernate.md`   | Session/persistence, N+1, second-level cache                |
| `junit5.md`      | Lifecycle, parameterized, extensions, parallelism           |
| `micronaut.md`   | Compile-time DI, GraalVM-native, reactive event loop        |
| `kafka.md`       | Consumer/producer config, exactly-once, schema registry     |
| `flyway.md`      | Schema migrations, baseline strategies, drift detection     |

## Why deferred

See `../README.md` siblings. 1.0 ships skeleton + `15-clean-code-java.md`;
deep-dives land per-framework.

## Contributing

PRs welcome. Diátaxis "explanation" form.
